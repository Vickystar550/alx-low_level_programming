#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Description: this program print to the stdout using puts()
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char myString[] = "\"Programming is like building a multilingual puzzle";

	puts(myString);

	return (0);
}
